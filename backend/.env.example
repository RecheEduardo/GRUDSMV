# Porta em que o servidor Express sobe
PORT=3000

# Segredo usado para assinar os tokens JWT (troque em producao)
JWT_SECRET=dev-secret-troque-em-producao

# Validade dos tokens JWT
JWT_EXPIRES_IN=7d
